[tools]
"cargo:cargo-dist" = "0.32.0"

[tasks.release-notes]
description = "Print GitHub release notes from dist host JSON"
run = "dist host --steps=create --output-format=json | jq -r .announcement_github_body"
