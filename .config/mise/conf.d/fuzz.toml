# cargo-fuzz must be installed from source with stable (mise installs incorrect musl build).
# The version is pinned below and tracked by renovate via customManagers in renovate.json.
[tasks.install-fuzz]
run = "RUSTUP_TOOLCHAIN=stable mise install 'cargo:cargo-fuzz@0.13.1'"
