Benchmarks for uglifying and prettifying using various CLI tools

You can run `just bench` to spin up a docker container that installs all the tools, most of which are installed via mise for ease of use

If you know of any tools that format or minify JSON, feel free to open a PR!
